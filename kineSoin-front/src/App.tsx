import './App.css';
import PublicLayout from './layouts/PublicLayout';

function App() {
  // const location = useLocation();
  // const { setError } = useAppContext();

  // useEffect(() => {
  //   setError(null);
  // }, [location.pathname]);

  return (
    <>
      <PublicLayout />
    </>
  );
}

export default App;
