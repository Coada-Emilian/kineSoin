import './App.css';

function App() {
  // const location = useLocation();
  // const { setError } = useAppContext();

  // useEffect(() => {
  //   setError(null);
  // }, [location.pathname]);

  return <>kineSoin V2.0</>;
}

export default App;
